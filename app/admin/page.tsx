import React from "react";
import AuthWrapper from "@/components/units/AuthWrapper";

type Props = {};

const AminPage = async (props: Props) => {
  return (
    <div>
      <AuthWrapper />
    </div>
  );
};

export default AminPage;
